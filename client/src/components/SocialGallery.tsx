import assets from '@/lib/socialAssets.json';
export default function SocialGallery({videosOnly=false}:{videosOnly?:boolean}){const posts=assets.filter(a=>a.href && /\/(p|reel)\//.test(a.href) && (!videosOnly || a.alt.startsWith('Video')));return <section className="social-gallery-section"><div className="signal-section-label"><span>FROM THE CLUB’S INSTAGRAM</span><a href="https://www.instagram.com/chiptech_rvu/" target="_blank" rel="noreferrer">@chiptech_rvu ↗</a></div><div className="social-grid">{posts.map(a=><a href={`https://www.instagram.com${a.href}`} key={a.local} target="_blank" rel="noreferrer"><img src={a.local} alt={a.alt.split('. May be')[0]} loading="lazy"/><span>{a.alt.split('. May be')[0]} <b>{a.alt.startsWith('Video')?'Watch reel ↗':'View post ↗'}</b></span></a>)}</div><p className="signal-caption">Posts and reel previews from the club’s public profile. Collaborative posts retain their original account attribution. Open a post to view its full caption or play the reel on Instagram.</p></section>}
